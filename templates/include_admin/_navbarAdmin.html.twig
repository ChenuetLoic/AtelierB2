{% block navBar %}
    <nav class="navbar navbar-expand-xl navbar-dark bg-dark fixed-top">
        <div class="container-fluid">
            <a class="navbar-brand d-none d-lg-block text-primary" href="{{ path('admin_index') }}">Atelier B2 Adeline Bellot</a>

            <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarNavDropdown"
                    aria-controls="navbarNavDropdown" aria-expanded="false" aria-label="Toggle navigation">
                <span class="navbar-toggler-icon"></span>
            </button>
            <div class="collapse navbar-collapse justify-content-end text-light" id="navbarNavDropdown">
                <ul class="navbar-nav">
                    <li class="nav-item mx-3">
                        <a class="nav-link text-primary" href="{{ path('carousel_index') }}">Carrousel</a>
                    </li>
                    <li class="nav-item mx-3">
                        <a class="nav-link text-primary" href="{{ path('picture_index') }}">Images Projet</a>
                    </li>
                    <li class="nav-item mx-3">
                        <a class="nav-link text-primary" href="{{ path('projets_index') }}">Projets</a>
                    </li>
                    <li class="nav-item mx-3">
                        <a class="nav-link text-primary" href="{{ path('about_admin') }}">À propos</a>
                    </li>
                    <li class="nav-item mx-3">
                        <a class="nav-link text-primary" href="{{ path('prestation_admin') }}">Prestation</a>
                    </li>
                    <li class="nav-item mx-3">
                        <a class="nav-link text-primary" href="{{ path('home') }}">Accueil</a>
                    </li>
                    {% if app.user %}
                        <li class="nav-item mx-3">
                            <a class="nav-link text-primary mx-3" href="{{ path('app_logout') }}"> Se déconnecter</a>
                        </li>
                    {% endif %}
                </ul>
            </div>
        </div>
    </nav>
{% endblock %}
