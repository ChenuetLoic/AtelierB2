<form method="post" action="{{ path('pictures_projet_delete', {'id': pictures_projet.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ pictures_projet.id) }}">
    <button class="btn">Delete</button>
</form>
