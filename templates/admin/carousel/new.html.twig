{% extends 'layout_admin.html.twig' %}
    {% block stylesheets %}
        {{ encore_entry_link_tags('admin') }}
    {% endblock %}
{% block title %}Nouveau|Carousel{% endblock %}

{% block body %}
<div class="container">
    <h1 class="text-center mt-4">Ajouté une nouvelle images</h1>
    {{ include('admin/carousel/_form.html.twig') }}
    <div class="text-center">
        <a class="btn btn-secondary btn mb-4" href="{{ path('carousel_index') }}">Retour</a>
    </div>
</div>
{% endblock %}
